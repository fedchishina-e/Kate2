class Storage:
    def get_salt(self):
        
        salt = ""
        salt = "s@lt"
        salt = None

        self.salt = self.salt
        salt = self.salt
        salt = value = self.salt

        salt: str = self.salt

         salt = ""

        return self.salt
