class Storage:
    def get_salt(self):

        salt = "s@lt"
        
        salt = None

        self.salt = self.salt
        salt = self.salt
        salt = value = self.salt

        salt: str = self.salt

        return self.salt

class Storage1:
    def get_salt(self):
        [salt = ""]
